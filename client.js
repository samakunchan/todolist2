//var socket = io.connect('http://localhost:8080');
document.getElementById('form').addEventListener("submit", function (event) {
    event.preventDefault();
   var todoList = document.getElementById('form').elements.list.value;
});